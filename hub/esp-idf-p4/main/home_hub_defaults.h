#pragma once

#include "bluepaws/map_engine.h"

namespace bluepaws::defaults {

// Shared Sandhurst, Gloucestershire starter grid. It keeps the hub and
// simulated collars visible together until a real positioning source wins.
inline constexpr map::GeoPoint kStarterLocation{51.905857, -2.239923};

}  // namespace bluepaws::defaults
